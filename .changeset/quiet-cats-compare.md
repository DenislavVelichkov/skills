---
"mattpocock-skills": minor
---

Add a fail-closed visual acceptance protocol across Wayfinder, specification,
ticketing, implementation, review, and project setup. Prototype references,
production candidates, explicit approval requests, human approval, and
promoted baselines now remain separate, hash-bound states. Missing initiative
manifests are created and validated as truthful planning artifacts before
implementation tickets are drafted or production code is edited. The protocol
applies only when the source requires a production-to-reference comparison,
requires the reference-selection action that enables one, or links an existing
manifest that records either obligation. Ordinary UI work creates no manifest.
